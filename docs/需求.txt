# 三维重建渲染器
1. 能够良好渲染高斯泼溅模型
使用playcanvas进行高斯喷溅的渲染
2. 展示时需要展示出模型的立体感（前后左右小幅度旋转）
通过对摄像头的控制实现，当用户手动干预时停止播放，五秒后重新播放
3. 渲染器接口扩展良好，方便后续作为纯渲染器接收参数进行渲染
可以通过json文件进行资产和路由的增添，通过url进行渲染需要的资产
4. 需要有信息展示页签，展示作品的一些信息
展示作品名称，背书单位，作者，创建时间。
5.背景看是否可以替换为全景底图
背景图使用hdr格式的文件。hdr格式可以通过ps等工具制作



